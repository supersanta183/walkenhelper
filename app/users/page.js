/*
import React from 'react'

async function page() {
  const res = await fetch(`http://localhost:3000/api/users`);
  const data = await res.json();

  return (
    <div>
      {data.map(user => (
        <div key={user.id}>
          <h1>{user.name}</h1>
        </div>
      ))}
    </div>
  )
}

export default page
*/